{
  "name": "sane",
  "private": true,
  "version": "0.4.0",
  "scripts": {
    "dev": "tauri dev",
    "build:backend": "cd backend && go build -ldflags \"-H windowsgui -s -w\" -o ../src-tauri/binaries/sane-backend-x86_64-pc-windows-msvc.exe .",
    "build:installer-images": "node make-installer-images.js",
    "build": "npm run build:installer-images && npm run build:backend && tauri build"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@tauri-apps/cli": "^2"
  }
}
